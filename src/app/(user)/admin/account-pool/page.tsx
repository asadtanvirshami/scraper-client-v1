import AccountPoolingManager from "@/features/admin/account-pooling";

export const metadata = { title: "Account Pooling" };

export default function AccountPoolPage() {
  return <AccountPoolingManager />;
}
